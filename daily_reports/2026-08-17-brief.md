# 26/08/17

今日完成 Project-Kouro 实验工作区搭建，打通 RoboCasa365 `TurnOffSinkFaucet` 的数据缓存、
rotation 6D state 转换、Diffusion Policy / ACT / Pi0.5 训练、周期 rollout、TensorBoard 和最终
success rate 评测闭环，并固定三套 20k steps、batch size 32 的 baseline。Pi0.5 改用 pinned
`lerobot/pi05_base`，加入权重完整性校验和 fail-closed 加载，避免静默随机初始化；同时定位并
解决阿里云 DSW 默认 tmux server 导致的 `server exited unexpectedly`，改为实验独立 socket。
目前 DP 和 ACT 已正常训练；Pi0.5 已完成首次缓存和 preflight，首次启动暴露的 pretrained
path/discovery 参数冲突也已定位修复，待重新启动验证 strict weight load 和首个 training step。
下一步将监控三套训练和周期评测，完成 50-episode 正式 success rate 对比，并据此确定首个
模型改进方向。
